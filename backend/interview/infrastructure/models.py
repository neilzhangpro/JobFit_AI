"""SQLAlchemy ORM models for interview_preps table.

This module contains SQLAlchemy ORM models for persisting interview
preparation data. All models must include tenant_id for multi-tenant isolation.
"""

# TODO: Implement InterviewPrepModel SQLAlchemy model
# TODO: Include fields: id, tenant_id, optimization_session_id, created_at, updated_at
# TODO: Add relationship to interview_questions table (if separate table)
# TODO: Add relationship to cover_letters table (if separate table)
# TODO: Ensure tenant_id is NOT NULL and indexed
# TODO: Add foreign key constraints where appropriate
