"""IOptimizationRepository interface (ABC)."""

# TODO: Define IOptimizationRepository abstract base class
#   - Methods: save(session), find_by_id(id), find_by_resume_id(resume_id)
#   - All methods must enforce tenant_id scoping
#   - Use Protocol or ABC for type safety
