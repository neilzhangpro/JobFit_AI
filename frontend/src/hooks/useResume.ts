/**
 * Custom hook for resume CRUD operations (upload, list, delete).
 */
export function useResume() {
  // TODO: Implement resume operations
  return {
    resumes: [],
    upload: async () => {},
    loading: false,
  };
}
